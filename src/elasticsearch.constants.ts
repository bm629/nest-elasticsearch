/**
 * this constant used as provider name which stores the value of elasticsrarch connection provider name
 * @type { string }
 * @constant
 */
export const ELASTICSEARCH_CONNECTION_NAME: string = 'ElasticsearchConnectionName';

/**
 * this constant us used as provider default provider name which has elasticsearch client value
 * @type { string }
 * @constant
 */
export const DEFAULT_ELASTICSEARCH_CLIENT_CONNECTION: string = 'ElasticsearchClientConnection';

/**
 * this constant is used as provider name which used to store {@link ElasticsearchModuleOptions} options
 * its only used when options passed asynchronsaly
 * @type { string }
 * @constant
 */
export const ELASTICSEARCH_MODULE_OPTIONS: string = 'ElasticsearchModuleOptions';
